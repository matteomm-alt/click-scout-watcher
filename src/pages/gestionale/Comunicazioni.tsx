import { Megaphone } from 'lucide-react';
import PlaceholderPage from '@/components/PlaceholderPage';

export default function Comunicazioni() {
  return (
    <PlaceholderPage
      block="gestionale"
      icon={Megaphone}
      title="Comunicazioni"
      description="Bacheca interna: avvisi, comunicazioni urgenti, conferme di lettura per atleta."
    />
  );
}
