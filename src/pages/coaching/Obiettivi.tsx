import { Target } from 'lucide-react';
import PlaceholderPage from '@/components/PlaceholderPage';

export default function Obiettivi() {
  return (
    <PlaceholderPage
      block="coaching"
      icon={Target}
      title="Obiettivi società"
      description="Obiettivi tecnici, tattici, fisici per stagione/fase. Stato e monitoraggio progressi."
    />
  );
}
