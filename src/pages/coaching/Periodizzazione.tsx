import { Workflow } from 'lucide-react';
import PlaceholderPage from '@/components/PlaceholderPage';

export default function Periodizzazione() {
  return (
    <PlaceholderPage
      block="coaching"
      icon={Workflow}
      title="Periodizzazione"
      description="Fasi della stagione (preparazione, competitivo, transizione) con obiettivi e carichi."
    />
  );
}
