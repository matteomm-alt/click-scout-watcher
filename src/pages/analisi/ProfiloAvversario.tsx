import { Shield } from 'lucide-react';
import PlaceholderPage from '@/components/PlaceholderPage';

export default function ProfiloAvversario() {
  return (
    <PlaceholderPage
      block="analisi"
      icon={Shield}
      title="Profilo avversario"
      description="Scheda completa dell'avversaria su più match: tendenze d'attacco, distribuzione, rotazioni deboli."
    />
  );
}
